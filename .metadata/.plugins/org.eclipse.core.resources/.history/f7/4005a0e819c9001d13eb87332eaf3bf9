$('#btn').on('click', function(){
	$.ajax({
    url:'/board', //request 보낼 서버의 경로
    type:'post', // 메소드(get, post, put 등)
    success: function(data) {
        //서버로부터 정상적으로 응답이 왔을 때 실행
    },
    error: function(err) {
        //서버로부터 응답이 정상적으로 처리되지 못햇을 때 실행
    }
});
});